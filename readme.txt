dbsync_wexl_prod is script to migrate only wexl_prod database


dbsync_strapi_prod is script to migrate only strapi_prod database


db_sync is whole script to migrate both wexl_prod and strapi_prod database in one single go.

....For now we are not migrating both at once....
